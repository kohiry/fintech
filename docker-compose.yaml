services: 
  api-gateway:
    build:
      context: .
    volumes:
      - .:/backend
    command: sh -c "uvicorn api_gateway.main:app --host 0.0.0.0 --port 8000 --reload"
    restart: always
    ports:
      - "8000:8000"    